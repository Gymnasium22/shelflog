/** ISO 4217 — белорусский рубль (действующая денежная единица РБ). */
export const DEFAULT_CURRENCY = "BYN" as const;

/** UI default locale (i18n-ready). */
export const DEFAULT_LOCALE = "ru" as const;

export const APP_NAME = "ShelfLog" as const;
